import { Grid, Typography } from '@mui/material';
import React from 'react';

const EditErrands: React.FC = () => {
  return (
    <Grid container sx={{ height: '100vh' }}>
      <Typography variant="h4"></Typography>
      <Grid item xs={7}>
      </Grid>
      <Grid item xs={5}>
      </Grid>
    </Grid>
  );
};

export default EditErrands;
